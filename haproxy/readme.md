HAProxy info

```
    Heart Beats:
    HAProxy will make health checks using the /hostname end point in all 3 instances of the message image. The health can be monitored easily by going to localhost/haproxy.

```